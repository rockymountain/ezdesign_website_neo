---
import BaseLayout from '../layouts/BaseLayout.astro';

import ProductHero from '../components/sections/ProductHero.astro';
import PainPointGrid from '../components/sections/PainPointGrid.astro';
import FeatureGrid from '../components/sections/FeatureGrid.astro';
import UseCaseGrid from '../components/sections/UseCaseGrid.astro';
import FeatureSpotlight from '../components/sections/FeatureSpotlight.astro';
import PricingGrid from '../components/sections/PricingGrid.astro';
import FAQGrid from '../components/sections/FAQGrid.astro';
import CTASection from '../components/common/CTASection.astro';
import Card from '../components/common/Card.astro';

import pageContent from '../data/ezd-ai-chat.json';
import pricingData from '../data/pricing.json';
import industriesData from '../data/industries.json';

type ChatMessage = {
  role: 'assistant' | 'user';
  text: string;
};

type PricingPlan = {
  name: string;
  priceLabel: string;
  description: string;
  features: string[];
  isRecommended?: boolean;
};

type Industry = {
  name?: string;
  title?: string;
  description: string;
};

const title = pageContent.seo.title;
const description = pageContent.seo.description;

const heroPreviewMessages = pageContent.hero.previewMessages as ChatMessage[];

const ezdAiChatPlans = pricingData.ezdAiChatPricing as PricingPlan[];
const pricingNote = pricingData.pricingNote;

const targetIndustries = industriesData.targetIndustries as Industry[];
---

<BaseLayout title={title} description={description}>
  <main class="min-h-screen bg-[var(--ezd-bg)] text-[var(--ezd-text)]">
    <ProductHero
      eyebrow={pageContent.hero.eyebrow}
      title={pageContent.hero.title}
      description={pageContent.hero.description}
      primaryCTA={pageContent.hero.primaryCTA}
      secondaryCTA={pageContent.hero.secondaryCTA}
      previewMessages={heroPreviewMessages}
      previewNote={pageContent.hero.previewNote}
    />

    <PainPointGrid
      eyebrow={pageContent.painPoints.eyebrow}
      title={pageContent.painPoints.title}
      description={pageContent.painPoints.description}
      items={pageContent.painPoints.items}
    />

    <FeatureGrid
      eyebrow={pageContent.difference.eyebrow}
      title={pageContent.difference.title}
      description={pageContent.difference.description}
      features={pageContent.difference.features}
      background="soft"
    />

    <section id="live-demo" class="relative overflow-hidden py-20">
      <div
        class="pointer-events-none absolute inset-0 bg-[linear-gradient(180deg,#ffffff_0%,var(--ezd-bg-soft)_100%)]"
        aria-hidden="true"
      >
      </div>

      <div class="ezd-container relative">
        <div class="grid gap-10 lg:grid-cols-[0.9fr_1.1fr] lg:items-center">
          <div>
            <p class="ezd-eyebrow">
              {pageContent.liveDemo.eyebrow}
            </p>

            <h2 class="ezd-heading-section mt-4 text-3xl md:text-5xl">
              {pageContent.liveDemo.title}
            </h2>

            <p class="ezd-body-large mt-5 text-[var(--ezd-text-muted)]">
              {pageContent.liveDemo.description}
            </p>

            <div class="mt-8 grid gap-3 sm:grid-cols-3">
              <div
                class="rounded-[var(--ezd-radius-xl)] border border-[var(--ezd-border)] bg-white p-4 shadow-[var(--ezd-shadow-sm)]"
              >
                <p class="text-xs font-black uppercase tracking-[0.18em] text-[var(--ezd-brand)]">
                  01
                </p>
                <p class="mt-2 text-sm font-black">Hỏi tự nhiên</p>
              </div>

              <div
                class="rounded-[var(--ezd-radius-xl)] border border-[var(--ezd-border)] bg-white p-4 shadow-[var(--ezd-shadow-sm)]"
              >
                <p class="text-xs font-black uppercase tracking-[0.18em] text-[var(--ezd-brand)]">
                  02
                </p>
                <p class="mt-2 text-sm font-black">Tư vấn theo ngành</p>
              </div>

              <div
                class="rounded-[var(--ezd-radius-xl)] border border-[var(--ezd-border)] bg-white p-4 shadow-[var(--ezd-shadow-sm)]"
              >
                <p class="text-xs font-black uppercase tracking-[0.18em] text-[var(--ezd-brand)]">
                  03
                </p>
                <p class="mt-2 text-sm font-black">Chuyển sang người thật</p>
              </div>
            </div>

            <p
              class="ezd-body-small mt-5 rounded-[var(--ezd-radius-lg)] border border-[var(--ezd-border)] bg-white p-4 text-[var(--ezd-text-muted)] shadow-[var(--ezd-shadow-sm)]"
            >
              AI hỗ trợ tư vấn ban đầu và có thể chưa chính xác trong mọi trường hợp.
              EZDesign không lưu nội dung hội thoại cá nhân và không dùng chat để thu thập thông tin nhạy cảm.
            </p>
          </div>

          <Card padding="lg" class="relative min-h-[460px] overflow-hidden border-dashed bg-white">
            <div
              class="pointer-events-none absolute -right-20 -top-20 h-56 w-56 rounded-full bg-[var(--ezd-accent)] opacity-20 blur-3xl"
              aria-hidden="true"
            >
            </div>

            <div
              id="ezd-ai-chat-demo"
              class="relative min-h-[390px] rounded-[var(--ezd-radius-2xl)] border border-[var(--ezd-border)] bg-[var(--ezd-bg-soft)] p-5"
            >
              <div class="flex items-center justify-between gap-4 border-b border-[var(--ezd-border)] pb-4">
                <div>
                  <p class="text-xs font-black uppercase tracking-[0.22em] text-[var(--ezd-brand)]">
                    Live Widget Area
                  </p>

                  <h3 class="mt-1 text-xl font-black tracking-tight">
                    {pageContent.liveDemo.placeholderTitle}
                  </h3>
                </div>

                <span
                  class="shrink-0 rounded-full bg-[var(--ezd-brand-soft)] px-3 py-1 text-xs font-black text-[var(--ezd-brand)]"
                >
                  Soon
                </span>
              </div>

              <div class="mt-5 space-y-4">
                <div
                  class="max-w-[82%] rounded-2xl bg-white px-4 py-3 text-sm leading-6 text-[var(--ezd-text-muted)] shadow-[var(--ezd-shadow-sm)]"
                >
                  Xin chào, mình là EZD Assistant. Bạn muốn xem demo theo ngành nào?
                </div>

                <div
                  class="ml-auto max-w-[82%] rounded-2xl bg-[var(--ezd-brand)] px-4 py-3 text-sm leading-6 text-white shadow-[var(--ezd-shadow-sm)]"
                >
                  Tôi muốn xem demo cho spa/salon.
                </div>

                <div
                  class="max-w-[82%] rounded-2xl bg-white px-4 py-3 text-sm leading-6 text-[var(--ezd-text-muted)] shadow-[var(--ezd-shadow-sm)]"
                >
                  Mình có thể mô phỏng flow tư vấn dịch vụ, đặt lịch và chuyển sang Zalo khi khách cần nhân viên hỗ trợ.
                </div>
              </div>

              <div class="mt-6 flex flex-wrap gap-2">
                <span
                  class="rounded-full border border-[var(--ezd-border)] bg-white px-3 py-1.5 text-xs font-black text-[var(--ezd-text-muted)]"
                >
                  Tư vấn dịch vụ
                </span>

                <span
                  class="rounded-full border border-[var(--ezd-border)] bg-white px-3 py-1.5 text-xs font-black text-[var(--ezd-text-muted)]"
                >
                  Đặt lịch demo
                </span>

                <span
                  class="rounded-full border border-[var(--ezd-border)] bg-white px-3 py-1.5 text-xs font-black text-[var(--ezd-text-muted)]"
                >
                  Chuyển sang Zalo
                </span>
              </div>

              <p class="ezd-body-small mt-6 text-[var(--ezd-text-soft)]">
                {pageContent.liveDemo.placeholderDescription}
              </p>
            </div>
          </Card>
        </div>
      </div>
    </section>

    <UseCaseGrid
      eyebrow={pageContent.useCases.eyebrow}
      title={pageContent.useCases.title}
      description={pageContent.useCases.description}
      items={targetIndustries}
    />

    <FeatureSpotlight
      eyebrow={pageContent.spotlight.eyebrow}
      title={pageContent.spotlight.title}
      description={pageContent.spotlight.description}
      features={pageContent.spotlight.features}
    />

    <PricingGrid
      eyebrow={pageContent.pricing.eyebrow}
      title={pageContent.pricing.title}
      description={pageContent.pricing.description}
      plans={ezdAiChatPlans}
      note={pricingNote}
    />

    <FAQGrid
      eyebrow={pageContent.faq.eyebrow}
      title={pageContent.faq.title}
      description={pageContent.faq.description}
      faqs={pageContent.faq.items}
    />

    <CTASection
      eyebrow={pageContent.finalCTA.eyebrow}
      title={pageContent.finalCTA.title}
      description={pageContent.finalCTA.description}
      primaryCTA={pageContent.finalCTA.primaryCTA}
      secondaryCTA={pageContent.finalCTA.secondaryCTA}
    />
  </main>
</BaseLayout>